# 复制为 packages/editor/.env.local 或 packages/renderer/.env.local（可选）
# API 基址；未设置时默认 http://<当前主机名>:3002
VITE_API_BASE=http://localhost:3002

# 发布态页面策略（编辑器，可选）
# strict：调试表只读，编辑/保存走发布表；须与后端 PAGE_POLICY=strict 成对启用
# VITE_PAGE_POLICY=strict
