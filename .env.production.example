# 生产构建示例 — 复制到 packages/editor/.env.production 与 packages/renderer/.env.production
# Vite 在 pnpm build 时读取；部署后修改须重新构建

# --- 公网 + 域名 ---
# VITE_API_BASE=https://api.example.com

# --- 内网 + IP（推荐：Nginx 静态 + API 直连 3002，见 doc/deploy-linux-nginx.md §0）---
# VITE_API_BASE=http://192.168.8.144:3002

# --- 内网 + IP（可选：API 经 Nginx 8082 反代，见 §1.1.1）---
# VITE_API_BASE=http://192.168.8.144:8082

# --- 内网 + IP（方案 B：单端口 80 + /api/ 反代）---
# VITE_API_BASE=http://192.168.1.100

# 仅 Editor 需要（与后端 PAGE_POLICY=strict 成对）
# VITE_PAGE_POLICY=strict
