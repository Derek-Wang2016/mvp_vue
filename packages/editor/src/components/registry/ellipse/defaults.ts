import { DEFAULT_ELLIPSE_PROPS } from '@mvp-vue/schema'

export function ellipseDefaultProps() {
  return { ...DEFAULT_ELLIPSE_PROPS }
}

export function ellipseDefaultSize() {
  return { w: 160, h: 160 }
}
